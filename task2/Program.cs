// Задача 2: Напишите программу, которая принимает на вход координаты точки (Х и Y), причём Х # 0 и У # О и выдаёт номер координатной четверти плоскости, в которой находится эта точка.
// Пример:
// 2, 3 ⇒ 1
// -5, 3  ⇒ 2
// -3, -2 ⇒ 3
// 4, -2 ⇒ 4

Console.Write("Введите X: ");
        int X = Convert.ToInt32(Console.ReadLine());
        Console.Write("Введите Y: ");
        int Y = Convert.ToInt32(Console.ReadLine());

        if (X > 0 && Y > 0)
        {
            Console.WriteLine("1");
        }
        else if (X < 0 && Y > 0)
        {
            Console.WriteLine("2");
        }
        else if (X < 0 && Y < 0)
        {
            Console.WriteLine("3");
        }
        else if (X > 0 && Y < 0)
        {
            Console.WriteLine("4");
        }
        else
        {
            // Так как по условию X и Y не могут быть равны 0, этот блок можно считать лишним.
            // Однако, чтобы обработать все возможные сценарии (включая ошибочный ввод),
            // можно добавить сообщение об ошибке или дополнительную логику.
            Console.WriteLine("Точка находится на одной из осей, что противоречит условиям задачи.");
        }